// Author: Stanley Wu
// global variables

// sets a new shell variable or replaces the variable with new_value if it already exists
int set(char name[], char new_value[]);
// print the shell varaible
int print(char name[]);
